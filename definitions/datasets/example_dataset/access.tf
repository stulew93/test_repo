# This file is the only file that needs editing for access to the dataset in this folder

locals {
  # Define the reader group access
  reader_group_by_email = [
  ]

  # Define the individual reader access
  reader_user_by_email = [
  ]

  # Define individual owner access
  owner_user_by_email = [
  ]

  # Define individual write access
  writer_user_by_email = [
  ]

  # Define authorised view access, this should be in the format "project.dataset.view"
  authorised_view = [
  ]

}
